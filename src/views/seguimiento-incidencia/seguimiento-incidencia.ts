import { Component, Prop, Vue } from "vue-property-decorator";

@Component({
  name: "SeguimientoIncidencia"
})
export default class SeguimientoIncidencia extends Vue {
    comentario = '';
    estado = true;
}
